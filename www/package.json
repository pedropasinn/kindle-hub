{
  "name": "missal-web",
  "private": true,
  "version": "0.1.0",
  "description": "Missal (Cordova legacy) rodando como site estático",
  "scripts": {
    "dev": "npx --yes http-server . -p 5173 -c-1",
    "start": "npx --yes http-server . -p 8080 -c-1"
  }
}
